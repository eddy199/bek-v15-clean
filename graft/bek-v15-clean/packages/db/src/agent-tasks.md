# bek-v15-clean/packages/db/src/agent-tasks.ts

- TaskKind · type · L16-L16 — type TaskKind = (typeof TASK_KINDS)[number];
- DirectKind · type · L26-L26 — type DirectKind = (typeof DIRECT_KINDS)[number];
- isDirectKind · function · L28-L30 — function isDirectKind(kind: string): kind is DirectKind
