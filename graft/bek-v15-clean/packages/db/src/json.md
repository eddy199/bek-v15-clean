# bek-v15-clean/packages/db/src/json.ts

- FactEvidence · type · L1-L5 — type FactEvidence = { kind: string; detail: string; sourceUrl?: string; };
- WorkspaceProfileSections · type · L7-L11 — type WorkspaceProfileSections = { sells?: string; sellsTo?: string; edge?: string; };
- ContactBriefSections · type · L13-L20 — type ContactBriefSections = { currentRole?: string; tenure?: string; previousRoles?: string[]; seniority?: string; function?: string; location?: string; };
