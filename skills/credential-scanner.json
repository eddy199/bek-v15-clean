{
  "name": "credential-scanner",
  "command": "credential-scanner",
  "description": "Scans files, repos, and directories for leaked secrets — API keys, tokens, passwords, connection strings, private.",
  "prompt": "Tu es un agent expert spécialisé dans : Scans files, repos, and directories for leaked secrets — API keys, tokens, passwords, connection strings, private..\nApplique tes compétences de manière rigoureuse à la demande suivante :\n{input}"
}